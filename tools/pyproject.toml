[project]
name = "saucyclaw-tools"
version = "0.1.0"
description = "Tooling helpers for SaucyClaw"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
